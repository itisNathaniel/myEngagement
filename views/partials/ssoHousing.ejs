<!-- Sessions Absent -->
<div class="modal" id="ssoHousingModal">
    <div class="modal-background"></div>
    <div class="modal-card box-width">
            <header class="modal-card-head">
              <p class="modal-card-title">SSO Housing</p>
              <button class="delete" aria-label="close"></button>
            </header>
            <section class="modal-card-body">
                    <table>
                            <tr class="individualSessionHead">
                                <th>Date</th>
                                <th>Site</th>
                            
                            </tr>
                        <% for(var i=0; i < ssoHousing.length; i++) { %>
                                <tr class="individualSession">
                                  <td><%= (ssoHousing[i])[3] %></td>
                                  <td><%= (ssoHousing[i])[2] // active state %> </td>
                                </tr>
                             <% } %>
                    </table>
            </section>
          </div>
    <button class="modal-close is-large" aria-label="close"></button>
</div>